/// [LibraryDisplayEnum] indicates the view that's activated.
enum LibraryDisplay {
  /// [list] view is active
  list,

  /// [grid] view is active
  grid,
}

/// [LibraryAppsEnum] indicates the view that's activated.
enum LibraryApps {
  /// [all] view is active
  all,

  /// [installed] view is active
  installed,
}
