import 'package:flutter/material.dart';
import 'package:hpos_appstore/utils/numericals.dart';

class Spacer {
  static Widget bottomMedium =
      const Padding(padding: EdgeInsets.only(bottom: Numericals.double16));

  static Widget bottomLarge =
      const Padding(padding: EdgeInsets.only(bottom: Numericals.double40));

  static Widget leftMedium =
      const Padding(padding: EdgeInsets.only(left: Numericals.double16));

  static Widget leftLarge =
      const Padding(padding: EdgeInsets.only(left: Numericals.double40));
}
