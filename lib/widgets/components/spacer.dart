import 'package:flutter/material.dart';
import 'package:hpos_appstore/utils/numericals.dart';

class Spacer {
  static Widget bottomMedium =
      Padding(padding: EdgeInsets.only(bottom: Numericals.double16));

  static Widget bottomLarge =
      Padding(padding: EdgeInsets.only(bottom: Numericals.double40));

  static Widget leftMedium =
      Padding(padding: EdgeInsets.only(left: Numericals.double16));

  static Widget leftLarge =
      Padding(padding: EdgeInsets.only(left: Numericals.double40));
}
